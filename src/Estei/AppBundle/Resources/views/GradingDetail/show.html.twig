{% extends '::base.html.twig' %}

{% block body -%}
    <h1>GradingDetail</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Percentage</th>
                <td>{{ entity.percentage }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('grading_detail') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('grading_detail_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
