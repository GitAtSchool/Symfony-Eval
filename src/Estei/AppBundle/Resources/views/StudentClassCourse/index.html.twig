{% extends '::base.html.twig' %}

{% block body -%}
    <h1>StudentClassCourse list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Studentclasscoursecol</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('student_class_course_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.studentClassCoursecol }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('student_class_course_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('student_class_course_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('student_class_course_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
